# HotelNGo 기능 계승 기준

Spotlog는 HotelNGo를 복제하지 않고 여행 경험에 필요한 개념만 독립적으로 재구현합니다.

| HotelNGo 기준 기능 | Spotlog 적용 | 처리 방식 |
|---|---|---|
| 여행 카드 | 적용 | 사용자가 소유한 여러 여행과 공개 여행일기 카드로 재정의 |
| 목적지·추천 메인 | 적용 | 홈에서 지역을 정해 일정을 찾거나, 목적지 없이 여행자 공개 가이드와 Spotlog 추천 일정을 자동 롤링·스와이프로 고른 뒤 내 여행으로 복사·수정 |
| 랜드마크·좌표 | 적용 | Spotlog 장소 계약으로 변환 |
| 랜드마크 발견 | 적용 | 홈과 분리된 `영상` 메뉴의 세로 피드로 한정. 숙소·맛집·카페는 쇼츠 피드에 노출하지 않음 |
| 추천 일정 초안 | 적용 | 저장 랜드마크의 거리·체류시간을 분석해 날짜별 순서, 도착 시각, 가이드 문단을 자동 생성. 결과는 내 비공개 여행기로 저장해 편집 |
| 날짜별 일정 | 적용 | 일기 본문, DAY 탭, 장소별 메모 타임라인으로 재구현 |
| 지도 동선 | 적용 | OSRM 도로 경로, 번호 마커, 경로 이동 애니메이션, 구간별 예상 이동시간·거리, 실패 시 직선거리 추정, 카카오맵 길찾기 연결 |
| 일정 시간 점검 | 적용 | 장소별 도착 시각·체류시간과 다음 구간 이동시간을 비교해 여유 또는 충돌 표시 |
| 일정 복사·공유 | 적용 | 여행 전체와 숙소·업체를 포함한 개별 장소 공유를 독립 구현 |
| 공개 여행 가이드 | 적용 | 다른 사람이 읽는 블로그형 상세 여행일기로 재정의 |
| 공개 일정 복사 | 적용 | 원본 작성자 출처를 보존한 독립 비공개 여행기 초안 생성 |
| 일정 편집기 | 적용 | 계획표 전용 UI가 아니라 글·사진·장소·숙소·맛집·카페 블록을 함께 다루는 여행기 편집기로 재구현 |
| 호텔 가격·객실 재고 | 제외 | 향후 HotelNGo 선택 연동 |
| 예약·결제·정산 | 제외 | Spotlog 핵심 범위 아님 |
| 파트너·호텔 관리자 | 제외 | 콘텐츠 운영 관리자만 별도 구축 |
| HotelNGo 회원 | 제외 | Spotlog 독립 회원 사용 |

Spotlog UI는 독립 모바일웹으로 구현하고 Expo 앱은 WebView 셸과 네이티브 기능 브리지를 담당합니다. HotelNGo의 HTML, CSS, `window`, `document`, `localStorage` 코드는 가져오지 않으며 `Trip → Day → Place`, 공개 가이드, 지도 순서 같은 여행 구성 개념만 Spotlog의 `Journey → JourneyDay → Place` 도메인으로 변환합니다.

호텔·식당·카페·로컬 업체를 전부 사전 데이터화하지 않습니다. 작성자가 이름, 지역, 주소, 사진, 기본 소개, 실제 경험과 추천 포인트를 직접 입력하는 것이 기본이며, 준비된 장소 목록과 향후 지도 장소 검색은 입력을 돕는 보조 수단입니다. 등록한 업체는 여행 기록의 일부로 공유할 수 있지만 실제 숙박비, 객실 재고, 예약, 결제 상태는 저장하거나 표시하지 않습니다.

여행기 본문의 `TEXT`, `IMAGE`, `PLACE` 블록 순서가 독자가 읽는 글의 순서이며, 같은 `PLACE` 블록은 날짜별 지도 동선에도 반영됩니다. 따라서 작성자는 “왜 이 장소를 갔는지”를 글로 설명하고 사진을 배치한 직후 해당 장소 카드를 삽입할 수 있으며, 독자는 글의 맥락과 이동 방향을 함께 볼 수 있습니다.

현재 경로 계산은 HotelNGo 프로토타입과 같은 단계적 방식입니다. 먼저 좌표만으로 즉시 예상 경로를 만들고, OSRM 도로 경로 응답이 도착하면 실제 도로선과 구간별 시간·거리로 교체합니다. 공개 OSRM 서버는 제품 운영용 SLA가 없고 실시간 교통도 반영하지 않으므로 운영 환경에서는 자체 호스팅 또는 국내 상용 길찾기 API로 교체해야 합니다.
