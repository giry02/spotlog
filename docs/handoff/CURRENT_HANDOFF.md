# Spotlog 현재 상태·복구 지침

기준일: 2026-09-09 / 원래 작업 ID: `01a00d5a-887a-70b2-93cf-260846464314`

## 1. 지금 요청된 일

사용자는 대화가 길어져 열리지 않을 경우를 우려하여, 인수인계용 새 대화와 작업 내용의 MD 저장을 요청함. 이번 작업은 복구 준비임. 서비스 기능 개발, APK 빌드, AI API 호출을 요청한 것이 아님.

직전 본 작업은 AI 계획에 내부 데이터 RAG·인기 순위·음식점/숙소 매칭과 여러 LLM의 적용 방법을 추가하는 일이었음. 해당 문서는 57쪽으로 반영·공개된 상태임. 일반 개발 계획은 32쪽이며, 3주 프런트엔드 개발 자체를 착수한 상태는 아님.

## 2. 저장소와 기준 파일

| 항목 | 확인값 |
|---|---|
| 원래 체크아웃 | `C:/Users/Giry/Documents/Spotlog` |
| 저장소 | https://github.com/giry02/spotlog |
| 기준 브랜치 | `codex/mobile-foundation` |
| 복구 작업 시작 전 HEAD | `b408341ea263af0ca9fa863f9725b8bc246f7a17` |
| 마지막 제품 관련 작업 | AI 보고서 모델 단가 표의 잘림 수정. 글씨를 줄이지 않고 열 너비 조정함 |
| 웹 | `web/` React/Vite 모바일웹 |
| 앱 | Expo 57 + React Native WebView 하이브리드 셸 |
| 주 진입 | 루트 `App.tsx` → `src/hybrid/HybridShell.tsx` |
| 이전 네이티브 시안 | `src/screens/`는 현재 주 서비스 진입 화면이 아님 |

`AGENTS.md`를 먼저 읽음. Expo 관련 코드 작성 전 `https://docs.expo.dev/versions/v57.0.0/`의 정확한 버전 문서를 확인해야 함. 최신 버전이라고 다른 버전을 임의 적용하지 않음.

## 3. 절대로 섞으면 안 되는 변경

인수인계 시작 당시 아래 변경이 원래 체크아웃에 남아 있음. 사용자 승인에 따라 **로컬에서만 보여 주는 사진 모션 실험**임.

- `web/src/App.tsx`: MotionPhotoReel, 사진 전환·진행 표시, 동영상/사진 모션 분기.
- `web/src/data.ts`: 선택 장소의 motionImages와 사진 지정.
- `web/src/styles.css`: 교차 전환·줌·이동 효과, 동작 줄이기 접근성 처리.
- `artifacts/`: 기존 시안 및 보고서 검수 산출물. 미추적 상태이며 일괄 공개 대상이 아님.

사진 모션은 브라우저 애니메이션이며 완성된 MP4 변환 기능이 아님. 공개 영상 화면을 이 실험으로 바꾸지 않음. 과거 관련 커밋 `1e807e8`은 `845b229`에서 공개본 반영이 취소되었음.

새 복구 작업은 위 미커밋 변경을 복제하지 않은 깨끗한 Git 작업 공간으로 준비함. 원래 체크아웃과 별도 백업에만 로컬 실험이 있음. 새 작업에서 파일이 보이지 않는다고 삭제된 것으로 단정하지 않음.

`git add -A`, 전체 초기화, 기존 변경 덮어쓰기 금지. 사용자는 별도 금지 전까지 완료된 변경을 커밋·푸시하도록 요청했지만, 로컬 전용 실험까지 공개하라는 뜻이 아님.

## 4. 현재 구현과 실제 서비스의 경계

| 구분 | 현재 상태 | 이후 필요한 일 |
|---|---|---|
| 홈·발견 | 추천/여행자 일정, 검색 진입, 장소 가이드·영상, 지역 필터 있음 | 콘텐츠 정확성·탐색 일관성 보완 |
| 저장·일정 | 지역별 저장, 여러 여행, DAY 편집, 장소 담기/해제, 여행기 복사 있음 | 이름 있는 보관함·상태 모델 정돈과 회귀 검증 |
| 여행기 | 글·사진·장소 블록, 직접 업체 입력, 미리보기·샘플 있음 | 임시 저장 복구·편집 품질과 사진 신뢰도 강화 |
| 지도 | MapLibre/OSM 표시, OSRM 도로 경로 및 실패 시 추정 표시 | 실제 도보·대중교통 공급자 연결·검증 |
| 도보 표시 | 자동차 경로 자료에서 도보 시간을 추정하는 부분이 있음 | 실제 보행 경로로 검증한 정보처럼 표시하지 않아야 함 |
| AI | 로컬 거리/체류시간 정렬과 정해진 안내 문구 | 실제 내부 RAG, 추천·경로·일정 엔진은 미구현 |
| 커뮤니티 | 댓글·반응·담김 수·등급·프로필·알림 관련 로컬 화면/데이터 | 다중 사용자 서버 집계·권한·신고·원격 푸시 미구현 |
| 저장소 | localStorage 및 로컬 이미지 처리 | 로그인·기기 동기화·미디어 서버·백업 미구현 |
| 외국인 | 별도 사진 이야기 시안 등에 언어 예시 있음 | 서비스 전체 번역·현지화 완료 상태가 아님 |
| Android | EAS APK/AAB 설정, WebView로 공개 웹 URL 로딩 | 최종 아이콘 선정·배포 검증·현장 기능은 별도 |

APK는 웹을 불러오는 하이브리드 셸임. 완전한 오프라인 네이티브 서비스로 설명하지 않음. 과거 사용자가 약 85MB라고 언급했지만, 현재 배포 파일 크기·최종 APK 주소를 이번 작업에서 다시 검증한 것은 아님. 새 빌드나 유료 EAS 작업을 자동 실행하지 않음.

## 5. 확정된 제품 방향

읽을 만한 여행기 → 장소 저장 → 내 일정에 활용 → 실제 여행 → 내 여행기 발행의 순환을 중심으로 함. 여행기는 글 속 사진·평가·현장 팁·숙소·음식점과 지도 동선이 연결되는 블로그형 가이드임.

- 여러 여행을 만들고, 먼저 읽은 뒤 복사·수정함. 보기 대신 수정 버튼부터 강요하지 않음.
- 국내 콘텐츠를 먼저 확보하고 영어 이용을 확장함. 한국어 원본은 유지함.
- 숙박비 비교·재고·예약·결제·정산은 포함하지 않음.
- 장소를 모두 미리 구축한다고 가정하지 않음. 직접 입력을 유지하고 외부 검색은 추후 연결함.
- 지도는 현재 방식 유지. 카카오 지도 전환은 사용자가 나중에 다시 처리한다고 명시함.
- 모바일에 맞는 읽을 수 있는 글자와 단순한 여행 브랜드 디자인을 유지함.
- DAY 탭 앞 뒤로가기 화살표를 유지함. 별도 고정 헤더를 추가해 높이를 차지하지 않음.
- 기간 2일과 장소를 넣을 DAY 2는 서로 다른 선택 상태로 관리함.
- 사진 모션을 승인 없이 공개 영상 화면에 넣지 않음. 풀영상과 사진 기반 형식의 병행 가능성은 유지함.

## 6. 개발 순서의 최신 결정

2~3명 기준 1차 1주 + 2차 1주 + 3차 1주, 총 3주 프런트엔드 우선임. 백엔드는 그 이후임.

1. 1주차: 발견·저장·보관함·대상 여행/DAY 선택·상태/오류 정돈.
2. 2주차: 작성/자동 임시 저장·복구·글/사진/장소 편집·복사/이동·지도 연결.
3. 3주차: AI 입력/결과/후보 승인/부분 수정, 관광 안내, 한국어/영어 흐름을 샘플 응답으로 검증.
4. 이후 B1 사용자/데이터 → B2 장소/미디어/공개 콘텐츠 → B3 커뮤니티/안전 → B4 실제 AI/영어 → B5 앱 현장 기능/확장.

`FUTURE_ROADMAP.md`는 보존된 과거 자료임. 그 안의 백엔드 우선 순서는 위 최신 결정에 의해 변경됨. 보고서 제작이 완료되었다는 것과 계획에 있는 기능 개발이 완료되었다는 것을 혼동하지 않음.

## 7. AI 방향의 최신 결정 — 특히 중요

사용자가 원한 핵심은 선택 장소를 순서대로 배열하는 기능만이 아님. **Spotlog에 쌓인 여행기·장소·인기도 데이터를 검색하여 지역/취향에 맞는 랜드마크를 찾고, 가까운 인기 음식점·숙소를 함께 매칭하는 추천**임.

| 계층 | 역할 |
|---|---|
| 내부 DB·메타 필터 | 지역·분류·좌표·권한·확인일·사용자 조건으로 후보를 제한함 |
| RAG·검색 | 여행기/장소 설명에서 취향과 맥락에 맞는 근거를 찾음. 벡터 검색만 사용하지 않음 |
| 인기 순위·재정렬 | 실제 저장·복사·반응 등 로우 데이터를 지역/분류별로 보정해 계산함. LLM이 수치를 창작하지 않음 |
| 후보 조합 | 랜드마크 주변 음식점, 전체 여행의 숙박 거점을 조건에 맞춰 추천함 |
| 경로·일정 | 실제 이동수단별 시간으로 실행 가능성을 검사함. 복잡한 시간 제약에는 OR-Tools를 선택적으로 사용함 |
| LLM | 자연어 조건 이해, 후보 조합 보조, 근거 기반 설명·관광 질문 응답. 없는 장소/좌표/후기를 생성하지 않음 |

모드도 분리함: ① 지역·취향 추천은 내부 후보를 초안에 자동 조합 후 사용자 최종 승인 ② 저장한 곳 구성은 선택 장소만 사용하고 보충은 선택 사항 ③ 외부 후보는 사용자가 승인하고 내부 인기 데이터와 구분함.

초기 기술 후보는 PostgreSQL + PostGIS + pgvector, 키워드/메타/의미 검색 결합임. 임베딩은 text-embedding-3-small과 BAAI bge-m3 등을 비교하는 계획임. LLM은 GPT 한 모델로 확정하지 않았음. 계획서의 Sol·Sonnet·Gemini·Luna 후보를 같은 데이터로 품질/비용/지연 평가한 뒤 결정함. 제품 내부 모델 명칭과 실제 API 제공 여부·단가·계약은 도입 시 공식 문서로 다시 확인함.

OR-Tools는 추천 데이터베이스나 대화 AI가 아니라 시간·순서 제약을 계산하는 선택 도구임. 처음에는 규칙 + 실제 경로 검증으로 시작하고 복잡해질 때 도입함. AI 생성 검증 범위 1~7일/최대 30곳은 초기 제안이며 수동 여행을 제한하는 결정이 아님.

번역은 한국어 원문에 영어 사본을 연결함. UI 직접 관리와 콘텐츠 자동번역을 구분하고, 원문/용어집 버전·검수 상태·한글 주소·숫자 보존을 검사함. 자세한 모델 비교, 단가 조건, 처리 예시와 출처는 AI_GUIDE_PLAN.md 전체에 보존함. 이 인수인계에서 새로 단가를 검증하거나 API를 호출한 것은 아님.

## 8. 문서·공개 주소와 검증 이력

- 서비스: https://giry02.github.io/spotlog/
- 개발 계획서 32쪽: https://giry02.github.io/spotlog/spotlog-development-plan.html
- AI 계획서 57쪽: https://giry02.github.io/spotlog/spotlog-ai-guide-plan.html
- 보존된 확장 보고서: https://giry02.github.io/spotlog/spotlog-expansion-report.html
- 아이콘 비교: https://giry02.github.io/spotlog/brand/icon-comparison/

보고서 원본은 `web/public/reports/development-plan.js`, `ai-guide-plan.js`, `ai-recommendation-sections.js`임. 공통 화면은 `report.js`, `report.css`임. MD는 전체 내용의 보존 사본임. 이후 변경 시 원본/MD의 버전 차이를 명시하거나 함께 갱신함.

직전 작업에서 웹 빌드와 57쪽 구조, 기존 34쪽 주제 보존, 공개 웹 페이지 넘침 및 모바일 너비 검수를 수행했음. 모델 가격 표는 글씨 축소 없이 열 폭을 조정했음. maplibre 번들 크기 경고가 있었으나 빌드 실패는 아니었음. 최신 57쪽의 PDF를 별도로 출력 검수한 것은 아니므로 PDF 검수 완료라고 말하지 않음.

## 9. 복구 후 행동 순서

1. 이 문서와 이력, AGENTS.md를 읽고 브랜치/미커밋 파일을 확인함.
2. 최신 사용자 메시지가 기능 개발인지 질문인지 구분함. 대기 요청이면 변경 없이 준비 상태만 알림.
3. 기능 변경 전 관련 코드와 해당 계획 부분을 다시 읽음. 문서에 있다고 구현되어 있다고 가정하지 않음.
4. 원래 체크아웃의 로컬 실험은 별도 백업과 비교하고, 승인 없이 가져오거나 적용하지 않음.
5. 수정한 범위만 테스트/커밋/푸시함. 성공 여부와 실제 확인 범위를 보고함.
6. 작업이 끝날 때 이 인수인계의 기준일·완료/남은 일·공개 금지 사항을 갱신함. 전체 계획의 내용을 줄여서 대체하지 않음.

### 실행 참고

`npm run web`으로 모바일웹 개발 서버, `npm run web:typecheck`와 `npm run web:build`로 웹 검증, `npm run typecheck`로 앱 쪽 타입 검사를 실행할 수 있음. 실행 전 package.json의 현재 스크립트를 확인함. 빌드/실행 자체도 새 사용자 요청의 범위 안에서만 진행함.

## 10. 세션 용량 확인 기록과 한계

2026-09-09 08:45경 KST의 로컬 세션 메타데이터에서 context window 258,400 토큰, 직전 요청 입력 210,409 토큰(약 81%), 잔여 약 19%를 확인했음. 누적 JSONL 기록 파일은 약 322MiB였음. 누적 파일 크기, 현재 문맥, 계정 사용량 한도는 서로 다름.

이후 대화 자동 요약/압축으로 현재 입력 크기는 바뀔 수 있음. 09:00경 KST에 다시 확인한 입력은 102,628/258,400 토큰(약 40%)이었으며 누적 로그는 약 333MiB였음. 현재 문맥은 줄어도 누적 파일은 커질 수 있음. 이 값은 고정된 실시간 계기판 값이 아니라 해당 시점 기록임. 누적 사용 토큰 총합을 현재 문맥 사용량으로 계산하지 않음. 정확한 현재 수치를 다시 요청하면 최신 token_count 메타데이터만 읽으며 원문 로그나 비밀키를 출력하지 않음.

이번 백업은 파일·계획·작업 결정의 복구 수단임. 앱 자체 오류나 대화 서비스 장애가 절대로 발생하지 않음을 보장하는 기능은 아님. 브라우저 저장 여행 데이터는 별도 범위임.
