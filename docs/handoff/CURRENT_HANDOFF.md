# Spotlog 현재 상태·복구 지침

기준일: 2026-09-10 / 원래 작업 ID: `01a00d5a-887a-70b2-93cf-260846464314`

## 1. 지금 요청된 일

**가이드 게시 전 검수:** 공개용 빌드 통과. 320·390·460px에서 디자인/폰트 선택·넘침 없음·폰트 별도 링크를 확인하고 기존 계획서5종의 문서 로딩·선택·인쇄 버튼 활성 상태를 확인함. 게시용 제품 JS/CSS 결과 파일은 기존 `index-6zCcQ9FG.js`·`index-HQak8Ef7.css`와 동일하며 `web/src` 변경 없음. 이번에 새 인쇄/PDF 결과 전체를 다시 검수한 것은 아님.

**2026-09-13 가이드만 GitHub 게시 및 문서 모음 연결:** 사용자가 현재 가이드·폰트 디자인 가이드 업로드와 문서 모음 포함을 명시함. 공개 기준 `8313515ad8f90aa5c8bf05e30d12993a73521694`의 깨끗한 `Spotlog-Publish-20260911` 작업본에서 가이드 HTML·MD·개발 지침과 문서 모음의 디자인·폰트 탭만 선별함. 전체 디자인/폰트 기준 전환, 스크롤형 가이드 로딩 판정, 폰트 앵커, 모바일 탭 가로 이동을 포함함. 제품 `App.tsx`·`styles.css`·로컬 가이드 컴포넌트·타이포그래피 테스트·미공개 기능은 게시 대상에서 제외함. 통계18/12·관련 제목16/메타11은 로컬 검수 결과이며 현재 공개 서비스에 반영된 것으로 설명하지 않음. 배포 완료 여부와 결과는 후속 기록으로 확인함.

**2026-09-13 통계 글자·타이포그래피 적용 계약:** 후속 스크린샷의 `누적 담김/공개 여행기` 설명9→12px, 숫자14→18px로 보완함. 기존 styles.css 토큰에 stat-value/stat-label 및 앞서 승인된 related-title/related-meta 별칭만 연결함. 기존2열·폭·padding10px12px·R11·색상·서체·행간은 유지하고 높이는 글자에 맞춰43.8→50.6px로 자연 증가함. §3 가이드에서 신규 기본값과 보존 예외를 명확히 구분함(본문16px·일반 안내14px·보조12px, 지정된 짧은 메타만11px; 신규 정보성8~10px 금지). 기존 카드·미디어의 역할별 값과 한글 대체 서체 정책을 유지하고 남은 작은 글자는 별도 검수 후보로 기록함. HTML/로컬 가이드도 같은 규칙·표로 갱신하고 이전 캡처의 기준일과 최신 글자 규칙일을 구분함. AGENTS.md에 적용 절차와 검사 명령을 기록함. `node --test web/tests/typography.test.cjs`7건 및 웹 빌드 통과, 통계/HTML 가이드320·390·460px 넘침 없음·통계 사진/폭/여백 유지 확인. 자동 검사는 핵심 토큰·작성자 통계/목록·가이드 동기화를 대상으로 하며 사이트 전체의 과거 작은 글자를 이미 모두 고친 것으로 설명하지 않음. 가이드 로컬 주소: `http://127.0.0.1:5191/spotlog-design-guide.html#sg-typography`. 사용자 데이터 보존. 커밋·푸시·배포 없음.

**2026-09-13 로컬 글자 크기 수정:** 사용자 요청으로 상세 하단 `이 작성자의 여행기` 목록의 제목을11→16px, 지역·기간/담김 수를8→11px로 조정함(`web/src/styles.css`의 `.creator-journey-list strong/small/em` 세 선언만 변경). 출처11px·구역 제목12px·사진64×58px·카드 padding7px/gap10px/R13·말줄임·구조는 유지함. 320/390/460px 수정 전후 카드 폭·사진·여백 동일, 넘침 없음, 다른 여행으로 이동 및 출처 팝업 열기/Esc 닫기를 확인함. 검수용 새 로컬 origin `http://127.0.0.1:5191/`을 사용하여 사용자 기존 저장 데이터는 건드리지 않음. 디자인 가이드에 역할별 승인값을 기록했으며 커밋·푸시·배포하지 않음. 기존 공개본/공개 사본은 아래2026-09-11 상태를 유지함.

**2026-09-11 선택 공개 완료:** 커밋 `8313515ad8f90aa5c8bf05e30d12993a73521694`를 `origin/codex/mobile-foundation`에 푸시했고 [Pages 배포](https://github.com/giry02/spotlog/actions/runs/34539353991)가 성공함. 공개 범위는 아래 승인대로 문서 변경·문서 모음과 여행기의 AI 추천 탭·여행기4개·사진11개·출처 보존만임. 공개 주소는 [여행기](https://giry02.github.io/spotlog/#community), [문서 모음](https://giry02.github.io/spotlog/spotlog-documents.html)임. 공개 사이트에서 탭4개 결과·사진·출처 팝업·문서5종 전환을 확인함. 로컬 제품 비교39건/문서326건과 별개로, 공개 사이트 검사에서 기존 MapLibre worker 파일404를 발견함. 이전 공개 커밋 `f5db559`와 이번 공개본의 지도 JS SHA256가 동일함(`C491D1A0966368C8F1FEE344C8E5FAEF59387D22634E72D7FE420A794B77F7A0`). 지도 정상 검수를 통과했다고 주장하지 않으며, 요청 밖 공통 지도 수정은 하지 않음. 근거: `artifacts/publication-live/results.json`, `artifacts/ai-publication-qa/results.json`, `artifacts/ai-publication-documents-qa/results.json`.

**선별 공개 후 로컬 보존 상태:** 원본 `C:/Users/Giry/Documents/Spotlog`의 브랜치 HEAD는 `f5db5598d47f3532573696413eb9f1a2c199d7b2`에 그대로 두었고 기존 로컬 작업과 비어 있던 인덱스를 보존함. 원격 브랜치는 위 공개 커밋으로 앞서 있음. 공개 사본 `C:/Users/Giry/Documents/Spotlog-Publish-20260911`은 해당 커밋의 깨끗한 detached worktree이며, 기준 비교 사본은 `C:/Users/Giry/Documents/Spotlog-Publish-Baseline-20260911`임. 이후 원본에서 무조건 pull/reset/전체 add·push하지 말고 로컬 미공개 기능을 보존하면서 공개 기준을 구분해야 함. 이 완료 기록은 로컬 인수인계용이며 후속 공개 커밋을 추가하지 않음.

**2026-09-11 최신 — 선택 공개 승인 범위:** 사용자가 문서 변경 전체와 여행기 메뉴의 ‘AI 추천 여행’ 탭·공공자료 기반 여행기4개만 GitHub에 올리도록 승인함. 공개용 사본 `C:/Users/Giry/Documents/Spotlog-Publish-20260911`에서 기존 공개본을 기준으로 필요한 부분만 이식함. 홈 AI 생성, 저장·담기 재설계, 사진 피드/모션, 갤러리, 지역 안내 변경, 로컬 저장소·편집기 확장 및 네이티브 앱은 이번 공개 범위에서 제외함. 공공 사진11개와 출처 상세·복사 시 출처 보존은 여행기 제공에 필요한 의존성임. 출처 팝업은 `PublicSourceSheet.tsx`와 전용CSS로 분리하여 다른 화면의 전역 스타일을 가져오지 않음. 문서에 기재된 미공개 기능은 계속 계획 또는 로컬 구현으로 구분함. 아래 이전 ‘공개하지 않음’은 각 작업 시점의 이력이며 이번 승인 범위를 확대하지 않음. 실제 공개 완료는 GitHub 배포 결과로 확인함.

**2026-09-11 최신 — 독립 문서와 한 화면 문서 모음:** 사용자 요청으로 `web/public/spotlog-documents.html`을 추가함. 전체 계획서·AI 개발·데이터 수집·요약본 4개 상단 분류에서 같은 화면 안의 문서를 교체하며, 요약본은 개발/AI 2종을 유지함. 독립 HTML은 전체47쪽·AI67쪽·데이터32쪽·개발 요약19쪽·AI 요약12쪽임. 기존 4개 보고서의 본문·앵커·계산기·스타일은 유지하고 HTML 껍데기에 문서 모음/임베드 연결만 추가함(AI 요약의 오래된 5쪽 메타 설명은12쪽으로 정정). 데이터 수집 문서는 `docs/DATA_SUPPLY_AND_NAVER_PLAN.md`의 본문·표행·목록을 보존해 별도로 제작하고 `docs/handoff/DATA_SUPPLY_PLAN.md`에도 저장함. 원본에 남아 있던 ‘개발자별 역할이나 고정 주간 일정…’ 메타 문장만 이전 삭제 지시에 맞춰 제거함. 수급 계획을 새로 실행하거나 계약·권리를 확정한 것은 아님. 문서 전환 시 페이지·입력 상태 유지, 별도 열기, 선택 문서 전체 인쇄, 목차·방향키, 320/390/460px, 인쇄 레이아웃과 실패 상태를 검수함. 결과는 `artifacts/documents-hub-qa/results.json`에 보존함. 원본 재생성 도구는 `artifacts/build-data-supply-report.cjs`임. 현재 로컬 주소: `http://127.0.0.1:5173/spotlog-documents.html`. 제품 UI·사용자 저장 데이터는 변경하지 않았으며 커밋·푸시·배포 없음. 아래 기록은 이전 시점의 이력임.

**2026-09-10 최신 — TourAPI 수급 반영·개발 요약 순서 정리:** 개발 전체는 47쪽, 요약은 19쪽임. 전체 1–46쪽 앵커를 유지하고 47쪽에 TourAPI 장소·좌표 확보 후 권리가 확인된 사진을 내부 장소/사진 ID로 연결하는 수급안을 추가함. 무료 API의 한도·운영 승인, 1유형 출처 표시·상업/변경 허용과 3유형 변경 금지, 저장·전송·검수 비용을 구분함. 42쪽 권리 기준·46쪽 차수별 수급 기준 및 `docs/DATA_SUPPLY_AND_NAVER_PLAN.md` 3절에도 반영함. 기존 출처 버튼·하단 상세를 유지하며 실제 API 수집 구현은 아님. 요약은 공통 방향 1–7쪽 → 1차 8–11쪽 → 2차 12–15쪽 → 3차 16쪽 → 후속 17–18쪽 → 완료 기준 19쪽 순서임. 기존 18쪽의 항목을 보존하고 단계가 다른 표 행도 해당 차수로 이동함. 이전 요약의 숫자 앵커는 순서 변경으로 달라졌으므로 제목과 최신 목차를 기준으로 찾음. HTML·MD 보존본·문서 설명/캐시 버전을 함께 갱신함. 전체 35쪽 ‘차수별 기록와 완료 정의’는 ‘차수별 기록과 완료 정의’로 정정함. 두 문서 66쪽의 데스크톱·320/390/460px 총264건, 인쇄 레이아웃·목차·방향키·이전/다음·연결 링크·기존 요약 표/주석 보존을 확인함(`artifacts/tourapi-plan-order-qa/results.json`). 제품 코드·스타일·사용자 데이터·AI 문서는 이번에 변경하지 않았으며 커밋·푸시·배포 없음.

추가로 사용자가 단독 개발과 백엔드 협업에 대한 의견을 요청함. 화면·제품 흐름은 직접 주도하고 서버 데이터 규격은 조기에 검토하되, 실제 운영 백엔드는 작은 저장·동기화 단위로 협업을 시험해 보도록 권고함. 이는 확정된 인력 배정이나 일정 변경이 아님. 기존 보고서에 개발자 A/B 역할·고정 주수·개인 개발 기준 문구를 다시 넣지 않음.

**2026-09-10 최신 — 중복 캡션 출처 제거·수급안 기록:** 사용자 승인으로 공공자료 샘플 캡션에서 표시용 출처 접미문구를 제거함. `사진 출처` 버튼/상세의 기관·저작자·이용 조건·날짜·원문 링크는 유지함. 새 샘플은 캡션에 설명만 저장하고, 기존 저장/복사본은 `photoCaptionWithoutDuplicateCredit`가 연결된 출처와 일치하는 알려진 접미문구만 표시에서 제외함. 사용자 저장 데이터는 덮어쓰지 않음. `StoryPhoto`가 기존 figure/캡션 역할과 앞서 조정한 간격을 그대로 사용함. `docs/DATA_SUPPLY_AND_NAVER_PLAN.md` 6절과 `docs/PUBLIC_CONTENT_SOURCES.md`에 출처 메타 분리·버튼/하단 상세·기존 기록 보존·갤러리/재사용/비대화형 내보내기 요건을 기록함. 공개 샘플/기존 개인 복사본을320/390/460px에서 검수하여 중복 없음·팝업 정보 보존·저장된 원본 불변을 확인함(`artifacts/photo-caption-qa/results.json`). 해당 함수 단위검사4건 통과. 커밋·푸시·공개 배포 없음. 아래 ‘아직 삭제하지 않음’은 이전 질문 시점의 기록임.

**2026-09-10 후속 — 출처 주변 세로 여백:** 첫 번째 사진→캡션 간격을 기준으로 출처가 붙은 캡션만 아래padding7px, 커버 출처가 있는 상세만 위padding9px·첫 출처margin-top0을 적용함. 320/390/460px에서 기준 글자 간격15px, 캡션→출처15.64px(기존23.64), 커버→출처15.20px(기존36.20)를 확인함. 폰트·버튼 크기·출처 없는 화면·팝업 동작은 유지함. `artifacts/source-spacing-qa/{before,after}.json`과 캡처, 웹 빌드 통과. 추가 질문 ‘캡션에도 출처를 중복해야 하는지’는 공식 공공누리1유형 이용조건으로 검토하여 답변함. 중복 캡션 삭제나 버튼 이름 변경은 아직 실행하지 않았음. 공공누리 공식 조건은 출처/저작권자와 가능한 원문 링크를 요구하지만 팝업만으로 충족한다는 명시는 없으므로 보장하지 않음. 로컬만 수정, 커밋·푸시·배포 없음.

**2026-09-10 최신 갱신 — 홈 여백·사진 출처:** 홈 소개 아래 AI 카드 간격을 기존 섹션 기준인 30px로 맞춤(`home-lead` 아래22px + 인접 카드 위8px). AI 카드 아래 추천 섹션30px·아래 검색 카드20px·폰트·카드 내부 크기는 유지함. 사용자 선택에 따라 `PhotoCredit`은 짧은 ‘사진 출처’ 버튼 → 기존 하단 팝업으로 변경함. 여행기 하단의 길고 좌측 정렬이 어긋난 출처 목록은 좌우20px의 ‘사진 출처 / N개 자료’ 행으로 정리하고, 사진 본문 출처는 캡션과 같은 좌우15px로 정렬함. 저작자·이용 조건·제작일·확인일·원문 링크·자료 안내는 상세에 보존함. AI/담기에서 출처를 열면 같은 패널로 상세 전환하며 원래 폼은 계속 유지함. `BottomSheet`의 상세 이력을 App의 일반 뒤로가기 처리가 가로채지 않도록 가드를 추가함. 320·390·460px 공개/담기/AI 출처 검수, 기존 카드/생성/제외 흐름17건, 단위 테스트49건, 웹 빌드 통과. 검수: `artifacts/home-spacing-qa/{before,after}.json`, `artifacts/photo-source-qa/results.json`, `artifacts/landmark-modal-qa/after/`. 사용자 데이터·기존 로컬 작업은 보존했으며 커밋·푸시·배포·APK 빌드 없음. 아래 기록은 각 시점의 과거 이력임.

**2026-09-10 최신 갱신 — 데이터 수급·네이버 연동·랜드마크 여러 사진 계획:** [DATA_SUPPLY_AND_NAVER_PLAN.md](../DATA_SUPPLY_AND_NAVER_PLAN.md)를 우선 읽음. 제미나이 공유본은 기존 스팟 중심 질문·답변 1쌍까지 다시 확인했으며 추가 데이터 문답이 보였다고 주장하지 않음. 공식 자료를 별도로 조사해 공공 개방·직접 제작·허가 UGC 기반 내부 장소 DB/RAG와 네이버 외부 검색을 분리함. 네이버 신규 신청은 API HUB이며 기존 개발자센터의 2026-09-07 검색 특약에 AI·저장·가공 제한이 있음. HUB의 정확한 적용 계약은 별도로 서면 확인해야 하며 사용자 선택만으로 영구 저장·RAG 권한을 얻었다고 처리하지 않음. 한도 문서의 월775,000/일25,000 불일치도 확인 대상으로 남김. 일반음식점 좌표 EPSG:5174, 개별 사진 권리·장소 일치·철회 전파를 기록함. **랜드마크 카드 사진 영역의 여러 이미지 가로 넘김**을 1차 추가 계획에 넣음. 지역 안내·AI·담기 카드가 공유하고, 저장 바깥의 작은 가로 카드와 기존 전체 화면 사진 피드는 유지함. 개발·AI 전체와 요약본에 함께 반영함. 이번에는 문서만 수정하며 제품 기능·API·이미지 수집을 새로 구현하지 않음. 커밋·푸시·배포 없음. 아래 문단은 각 시점의 과거 이력임.

**2026-09-10 최신 갱신 — 스팟 중심 계획과 팝업 사진 카드:** 공유 대화 검토 결과를 개발·AI 계획서 및 요약본 4종에 추가함. 주변 음식점·카페는 내 여행의 기준 스팟에서 하단 팝업으로 추가하고, 숙소는 숙박일 거점으로 다루는 계획임. 이 주변 업체 매칭 엔진 자체를 이번에 구현한 것은 아님. 실제 구현은 홈 AI 결과와 여행 담기 팝업의 `LandmarkGuideCard` 재사용, AI DAY별 사진 확인·방문별 제외/재포함·승인 저장, 담기 카드의 DAY 담김/해제 표시임. 저장 바깥 목록은 기존 작은 가로 카드로 유지함. 정상 카드의 사진205px/제목21px/설명13px/원래스타일을 유지하고 사진 실패만 동일 영역에서 안내함. 상세는 `LOCAL_TRAVEL_ADDITIONS.md`의 사진 카드 후속 절을 확인함. 이번 변경도 로컬 전용이며 커밋·푸시·공개 배포·APK 빌드 없음. 아래 ‘최신’ 문구들은 각 시점의 과거 이력임.

**2026-09-10 최신 기능 요청:** [LOCAL_TRAVEL_ADDITIONS.md](./LOCAL_TRAVEL_ADDITIONS.md)를 우선 확인함. 작은 저장 카드를 유지한 하단 담기 팝업에서 새 여행 기간·DAY를 선택하고 내 여행의 선택 DAY로 이동하도록 연결함. 홈에는 글 입력형 AI 로컬 추천 미리보기를 추가함. 여행기 메뉴의 AI 추천 여행은 여행자 기록과 구분하고, 공공누리 출처가 확인된 실제 사진·자료를 사용한 샘플을 제공함. 업체 검색 계획은 Kakao Local 대신 네이버 지역 검색으로 변경함. 실제 LLM·네이버 API 서버 연결은 아직 없음. 기능·문서 변경 모두 로컬 전용으로 커밋·푸시·배포하지 않음. 아래는 각 시점의 과거 기록이며 이번에 해소된 기간/DAY 결합·담기 권한 문제를 현재 미구현으로 다시 판정하지 않음.

**최신 추가 요청 — 디자인 가이드만 게시하고 1차 현황을 다시 보고함.** 공개 문서 주소는 `https://giry02.github.io/spotlog/spotlog-design-guide.html`임. 기능이 아니라 독립 HTML·문서·검수용 캡처만 게시하는 범위이며, 로컬 1차 기능·사진 실험·실제 서비스 코드는 공개하지 않음. 1차 재점검은 `PHASE_ONE_STATUS.md`를 확인함. F02~F04는 원래 저장으로 복원하면서 철회되어 미완료/재작업이며 AI 기간과 담을 DAY 결합·암묵적 대상 여행·소유 여부 확인 누락이 남아 있음. 이번에 남은 서비스 기능을 구현한 것은 아님. 아래 가이드 게시 전 기록의 “푸시 없음”은 과거 이력임.

**최신 지시 — 저장 한 화면이 아닌 현재 사이트 전체 화면을 기준으로 디자인 가이드를 작성함.** 홈·여행기/검색·장소 안내/영상/사진·저장·내 여행·공개/내 상세·DAY/지도·댓글/작성자·편집·프로필/설정·생성/장소/백업 팝업을 함께 조사함. `docs/STYLE_GUIDE.md`와 로컬 `#style-guide`를 공통 기준 → 전체 화면 캡처 → 역할별 부품 → 추가 작업 원칙으로 재구성함. 화면별 버튼·폰트·여백 차이를 보존하며 한 화면이나 helper 기본값으로 통일하지 않음. 이번 작업에서 제품 소스·CSS·사용자 데이터는 변경하지 않음. 사진 실험은 별도 구분, GitHub 커밋·푸시·배포 없음. 아래 저장 중심 가이드 기록은 과거 이력이며 전체 기준으로 사용하지 않음.

**2026-09-10 이후 갱신: 이 복원 디자인 자체를 기준으로 가이드를 확정함.** `docs/STYLE_GUIDE.md`와 로컬 `#style-guide`에 저장 카드·AI 카드·지역 칩의 실제 규격과 역할별 차이를 기록함. 가이드 제작을 이유로 제품 CSS·글꼴·토큰을 바꾸지 않음. 추가 사용자 지시로 `제주 랜드마크 4곳 샘플도 보기`와 빈 저장 목록의 샘플 여행 생성 경로를 제거함. 이제 저장한 장소로만 생성하며, 빈 목록에서는 생성이 비활성화됨. 아래 샘플 생성 설명은 제거 전 기록임. 커밋·푸시하지 않음.

**2026-09-10 최우선 갱신: 사용자가 저장 화면을 GitHub 버전으로 돌리라고 지시함.** 원격 HEAD `81a1085`를 확인하고 원본 `Saved`·`SavedPlaceCard`와 AI 카드·기간 선택·지역별 목록·DAY 담기/해제·제주 샘플 생성을 복원함. 로컬 저장→별도 여행/DAY 선택 화면과 저장 하단 백업 버튼은 철회함. `localRepository`의 최근 사용자 데이터는 그대로 보존함. 아래 저장 진입 설명은 철회 전 기록이며 다시 구현할 지시가 아님. 다른 로컬 작업·사진 피드·원래 스타일 복원은 유지함. 커밋·푸시·배포하지 않음.

최신 요청은 **기존 디자인을 유지하면서 1차 프런트엔드를 구현하되 GitHub에 올리지 않고 로컬에만 작업하는 것**임. 이전 자동 푸시 지침보다 이번 로컬 전용 지시를 우선함. 커밋·푸시·배포를 하지 않음.

1차 구현을 최신 사용자 지적에 맞춰 정리 중임. **저장은 단일 장소 목록과 담기/만들기/AI 진입 버튼만 제공함. 누르면 내 여행으로 먼저 이동하고 여행/DAY 선택과 생성 입력은 그곳에서 수행함. 확정하면 선택한 DAY의 내 여행 상세를 보여줌. 저장 안 여행/DAY 선택창·생성 폼과 별도 보관함 단계는 모두 거절된 방향이므로 다시 도입하지 않음.** 저장 카드는 지역 안내와 같은 큰 사진 카드임. 사진 피드는 장소 간 세로, 같은 장소 사진 간 가로 이동함. [PHASE_ONE_IMPLEMENTATION.md](./PHASE_ONE_IMPLEMENTATION.md)를 확인함. 로컬 주소는 `http://127.0.0.1:5173/`임.

**최신 스타일 지시: 사용자는 이전 디자인을 좋아하며 그 디자인을 기준으로 가이드를 만들라고 한 것임. 새 글꼴·크기·버튼·여백으로 전역 재디자인한 직전 작업은 거절됨.** 기존 `styles.css`의 Manrope 제목·DM Sans 본문, 페이지 버튼 52px/15px 모서리/14px 글자와 장소 카드 버튼 46px/12px 모서리/12px 글자 등 문맥별 차이를 보존함. [STYLE_GUIDE.md](../STYLE_GUIDE.md)와 `#style-guide`는 기존 모습을 기록하는 자료로 정정함. `theme.css`는 기존 값을 재사용하는 별칭·공통 부품에만 적용하며 기존 화면을 전역 덮어쓰지 않음. 백업 팝업의 파일 선택 개선·붙은 여백 수정, 저장→내 여행에서 선택·생성하는 기능은 유지함. 과거 `style-guide-qa` 124개 통과는 거절된 디자인의 기록이므로 이번 복원의 검수 완료로 인용하지 않음.

사용자 요청에 따라 보고서/요약본의 저장·내 여행 방향도 갱신함. 개발 요약 11쪽·상세 36쪽, AI 요약 5쪽·상세 57쪽의 내용과 장수는 유지함. 문서도 로컬에서만 변경함. 2차 스레드/편집 고도화, 3차 실제 AI 준비, APK 재빌드 및 원격 배포는 이번 작업에 포함하지 않음. 작업 전부터 있던 사진 모션 실험은 유지함.

## 2. 저장소와 기준 파일

문서 갱신 검수 기록(2026-09-10): 개발 전체46쪽·요약18쪽, AI 전체67쪽·요약12쪽. 기존 페이지 순서와 앵커를 보존함. 총143쪽의 데스크톱 및 320·390·460px 화면572건·인쇄 레이아웃 검사, 이전/다음·방향키·목차·로컬 문서 연결 검사에서 오류 없음. 추가 페이지를 시각 확인함. 결과는 `artifacts/data-supply-reports-qa/results.json`, 갱신 전 데이터는 `artifacts/data-reports-before-20260910.json`임. 제품 기능 테스트나 Android 실기기 검수 완료를 뜻하지 않음. HEAD는 `f5db5598d47f3532573696413eb9f1a2c199d7b2`로 그대로이며 스테이징·커밋·푸시 없음.

| 항목 | 확인값 |
|---|---|
| 원래 체크아웃 | `C:/Users/Giry/Documents/Spotlog` |
| 저장소 | https://github.com/giry02/spotlog |
| 기준 브랜치 | `codex/mobile-foundation` |
| 복구 작업 시작 전 HEAD | `b408341ea263af0ca9fa863f9725b8bc246f7a17` |
| 마지막 제품 관련 작업 | AI 보고서 모델 단가 표의 잘림 수정. 글씨를 줄이지 않고 열 너비 조정함 |
| 웹 | `web/` React/Vite 모바일웹 |
| 앱 | Expo 57 + React Native WebView 하이브리드 셸 |
| 주 진입 | 루트 `App.tsx` → `src/hybrid/HybridShell.tsx` |
| 이전 네이티브 시안 | `src/screens/`는 현재 주 서비스 진입 화면이 아님 |

`AGENTS.md`를 먼저 읽음. Expo 관련 코드 작성 전 `https://docs.expo.dev/versions/v57.0.0/`의 정확한 버전 문서를 확인해야 함. 최신 버전이라고 다른 버전을 임의 적용하지 않음.

## 3. 절대로 섞으면 안 되는 변경

인수인계 시작 당시 아래 변경이 원래 체크아웃에 남아 있음. 사용자 승인에 따라 **로컬에서만 보여 주는 사진 모션 실험**임.

- `web/src/App.tsx`: MotionPhotoReel, 사진 전환·진행 표시, 동영상/사진 모션 분기.
- `web/src/data.ts`: 선택 장소의 motionImages와 사진 지정.
- `web/src/styles.css`: 교차 전환·줌·이동 효과, 동작 줄이기 접근성 처리.
- `artifacts/`: 기존 시안 및 보고서 검수 산출물. 미추적 상태이며 일괄 공개 대상이 아님.

사진 모션은 브라우저 애니메이션이며 완성된 MP4 변환 기능이 아님. 공개 영상 화면을 이 실험으로 바꾸지 않음. 과거 관련 커밋 `1e807e8`은 `845b229`에서 공개본 반영이 취소되었음.

새 복구 작업은 위 미커밋 변경을 복제하지 않은 깨끗한 Git 작업 공간으로 준비함. 원래 체크아웃과 별도 백업에만 로컬 실험이 있음. 새 작업에서 파일이 보이지 않는다고 삭제된 것으로 단정하지 않음.

`git add -A`, 전체 초기화, 기존 변경 덮어쓰기 금지. 사용자는 별도 금지 전까지 완료된 변경을 커밋·푸시하도록 요청했지만, 로컬 전용 실험까지 공개하라는 뜻이 아님.

## 4. 현재 구현과 실제 서비스의 경계

| 구분 | 현재 상태 | 이후 필요한 일 |
|---|---|---|
| 홈·발견 | 추천/여행자 일정, 검색 진입, 장소 가이드·영상, 지역 필터 있음 | 콘텐츠 정확성·탐색 일관성 보완 |
| 저장·일정 | 저장의 진입 버튼 → 내 여행에서 여행/날짜 선택·생성 → 확정한 DAY 상세. 저장 내 선택/생성 폼은 제거 | 편집 고도화·실제 서버 동기화. 보관함 단계를 다시 도입하지 않음 |
| 여행기 | 글·사진·장소 블록, 직접 업체 입력, 미리보기·샘플 있음 | 임시 저장 복구·편집 품질과 사진 신뢰도 강화 |
| 지도 | MapLibre/OSM 표시, OSRM 도로 경로 및 실패 시 추정 표시 | 실제 도보·대중교통 공급자 연결·검증 |
| 도보 표시 | 자동차 경로 자료에서 도보 시간을 추정하는 부분이 있음 | 실제 보행 경로로 검증한 정보처럼 표시하지 않아야 함 |
| AI | 로컬 거리/체류시간 정렬과 정해진 안내 문구 | 실제 내부 RAG, 추천·경로·일정 엔진은 미구현 |
| 커뮤니티 | 댓글·반응·담김 수·등급·프로필·알림 관련 로컬 화면/데이터 | 다중 사용자 서버 집계·권한·신고·원격 푸시 미구현 |
| 저장소 | 버전 2 로컬 저장, 이전 원본 보존, 파일 백업·복원 및 로컬 이미지 처리 | 로그인·기기 동기화·미디어 서버·서버 백업 미구현 |
| 외국인 | 별도 사진 이야기 시안 등에 언어 예시 있음 | 서비스 전체 번역·현지화 완료 상태가 아님 |
| Android | EAS APK/AAB 설정, WebView로 공개 웹 URL 로딩 | 최종 아이콘 선정·배포 검증·현장 기능은 별도 |

APK는 웹을 불러오는 하이브리드 셸임. 완전한 오프라인 네이티브 서비스로 설명하지 않음. 과거 사용자가 약 85MB라고 언급했지만, 현재 배포 파일 크기·최종 APK 주소를 이번 작업에서 다시 검증한 것은 아님. 새 빌드나 유료 EAS 작업을 자동 실행하지 않음.

## 5. 확정된 제품 방향

읽을 만한 여행기 → 장소 저장 → 내 일정에 활용 → 실제 여행 → 내 여행기 발행의 순환을 중심으로 함. 여행기는 글 속 사진·평가·현장 팁·숙소·음식점과 지도 동선이 연결되는 블로그형 가이드임.

- 여러 여행을 만들고, 먼저 읽은 뒤 복사·수정함. 보기 대신 수정 버튼부터 강요하지 않음.
- 국내 콘텐츠를 먼저 확보하고 영어 이용을 확장함. 한국어 원본은 유지함.
- 숙박비 비교·재고·예약·결제·정산은 포함하지 않음.
- 장소를 모두 미리 구축한다고 가정하지 않음. 직접 입력을 유지하고 외부 검색은 추후 연결함.
- 지도는 현재 방식 유지. 카카오 지도 전환은 사용자가 나중에 다시 처리한다고 명시함.
- 모바일에 맞는 읽을 수 있는 글자와 단순한 여행 브랜드 디자인을 유지함.
- DAY 탭 앞 뒤로가기 화살표를 유지함. 별도 고정 헤더를 추가해 높이를 차지하지 않음.
- 기간 2일과 장소를 넣을 DAY 2는 서로 다른 선택 상태로 관리함.
- 사진 모션을 승인 없이 공개 영상 화면에 넣지 않음. 풀영상과 사진 기반 형식의 병행 가능성은 유지함.

## 6. 개발 순서의 최신 결정 (2026-09-10 정정)

프런트엔드는 사용자가 혼자 개발함. 1차·2차·3차는 기능 구현 단위이며 주수나 일수로 고정하지 않음. 개발자 A/B/C 역할 배정은 삭제함. 각 차수 완료 조건을 확인하고 다음 차수로 진행하며 백엔드는 그 이후임.

1. 1차: 발견·단일 저장 목록·여행 생성·대상 여행/DAY 담기·상태/오류 정돈. 별도 보관함 분류는 도입하지 않음.
2. 2차: 작성/자동 임시 저장·복구·글/사진/장소 편집·복사/이동·지도 연결.
3. 3차: AI 입력/결과/후보 승인/부분 수정, 관광 안내, 한국어/영어 흐름을 샘플 응답으로 검증.
4. 이후 B1 사용자/데이터 → B2 장소/미디어/공개 콘텐츠 → B3 커뮤니티/안전 → B4 실제 AI/영어 → B5 앱 현장 기능/확장.

`FUTURE_ROADMAP.md`는 보존된 과거 자료임. 그 안의 백엔드 우선 순서는 위 최신 결정에 의해 변경됨. 이전의 3주 일정·다인 역할 분담 역시 이 결정으로 대체됨. 보고서 제작이 완료되었다는 것과 계획에 있는 기능 개발이 완료되었다는 것을 혼동하지 않음.

## 7. AI 방향의 최신 결정 — 특히 중요

사용자가 원한 핵심은 선택 장소를 순서대로 배열하는 기능만이 아님. **Spotlog에 쌓인 여행기·장소·인기도 데이터를 검색하여 지역/취향에 맞는 랜드마크를 찾고, 가까운 인기 음식점·숙소를 함께 매칭하는 추천**임.

| 계층 | 역할 |
|---|---|
| 내부 DB·메타 필터 | 지역·분류·좌표·권한·확인일·사용자 조건으로 후보를 제한함 |
| RAG·검색 | 여행기/장소 설명에서 취향과 맥락에 맞는 근거를 찾음. 벡터 검색만 사용하지 않음 |
| 인기 순위·재정렬 | 실제 저장·복사·반응 등 로우 데이터를 지역/분류별로 보정해 계산함. LLM이 수치를 창작하지 않음 |
| 후보 조합 | 랜드마크 주변 음식점, 전체 여행의 숙박 거점을 조건에 맞춰 추천함 |
| 경로·일정 | 실제 이동수단별 시간으로 실행 가능성을 검사함. 복잡한 시간 제약에는 OR-Tools를 선택적으로 사용함 |
| LLM | 자연어 조건 이해, 후보 조합 보조, 근거 기반 설명·관광 질문 응답. 없는 장소/좌표/후기를 생성하지 않음 |

모드도 분리함: ① 지역·취향 추천은 내부 후보를 초안에 자동 조합 후 사용자 최종 승인 ② 저장한 곳 구성은 선택 장소만 사용하고 보충은 선택 사항 ③ 외부 후보는 사용자가 승인하고 내부 인기 데이터와 구분함.

초기 기술 후보는 PostgreSQL + PostGIS + pgvector, 키워드/메타/의미 검색 결합임. 임베딩은 text-embedding-3-small과 BAAI bge-m3 등을 비교하는 계획임. LLM은 GPT 한 모델로 확정하지 않았음. 계획서의 Sol·Sonnet·Gemini·Luna 후보를 같은 데이터로 품질/비용/지연 평가한 뒤 결정함. 제품 내부 모델 명칭과 실제 API 제공 여부·단가·계약은 도입 시 공식 문서로 다시 확인함.

OR-Tools는 추천 데이터베이스나 대화 AI가 아니라 시간·순서 제약을 계산하는 선택 도구임. 처음에는 규칙 + 실제 경로 검증으로 시작하고 복잡해질 때 도입함. AI 생성 검증 범위 1~7일/최대 30곳은 초기 제안이며 수동 여행을 제한하는 결정이 아님.

번역은 한국어 원문에 영어 사본을 연결함. UI 직접 관리와 콘텐츠 자동번역을 구분하고, 원문/용어집 버전·검수 상태·한글 주소·숫자 보존을 검사함. 자세한 모델 비교, 단가 조건, 처리 예시와 출처는 AI_GUIDE_PLAN.md 전체에 보존함. 이 인수인계에서 새로 단가를 검증하거나 API를 호출한 것은 아님.

## 8. 문서·공개 주소와 검증 이력

- 서비스: https://giry02.github.io/spotlog/
- 개발 계획서 36쪽: https://giry02.github.io/spotlog/spotlog-development-plan.html
- AI 계획서 57쪽: https://giry02.github.io/spotlog/spotlog-ai-guide-plan.html
- 보존된 확장 보고서: https://giry02.github.io/spotlog/spotlog-expansion-report.html
- 아이콘 비교: https://giry02.github.io/spotlog/brand/icon-comparison/

보고서 원본은 `web/public/reports/development-plan.js`, `ai-guide-plan.js`, `ai-recommendation-sections.js`임. 공통 화면은 `report.js`, `report.css`임. MD는 전체 내용의 보존 사본임. 이후 변경 시 원본/MD의 버전 차이를 명시하거나 함께 갱신함.

직전 작업에서 웹 빌드와 57쪽 구조, 기존 34쪽 주제 보존, 공개 웹 페이지 넘침 및 모바일 너비 검수를 수행했음. 모델 가격 표는 글씨 축소 없이 열 폭을 조정했음. maplibre 번들 크기 경고가 있었으나 빌드 실패는 아니었음. 최신 57쪽의 PDF를 별도로 출력 검수한 것은 아니므로 PDF 검수 완료라고 말하지 않음.

## 9. 복구 후 행동 순서

1. 이 문서와 이력, AGENTS.md를 읽고 브랜치/미커밋 파일을 확인함.
2. 최신 사용자 메시지가 기능 개발인지 질문인지 구분함. 대기 요청이면 변경 없이 준비 상태만 알림.
3. 기능 변경 전 관련 코드와 해당 계획 부분을 다시 읽음. 문서에 있다고 구현되어 있다고 가정하지 않음.
4. 원래 체크아웃의 로컬 실험은 별도 백업과 비교하고, 승인 없이 가져오거나 적용하지 않음.
5. 수정한 범위만 테스트/커밋/푸시함. 성공 여부와 실제 확인 범위를 보고함.
6. 작업이 끝날 때 이 인수인계의 기준일·완료/남은 일·공개 금지 사항을 갱신함. 전체 계획의 내용을 줄여서 대체하지 않음.

### 실행 참고

`npm run web`으로 모바일웹 개발 서버, `npm run web:typecheck`와 `npm run web:build`로 웹 검증, `npm run typecheck`로 앱 쪽 타입 검사를 실행할 수 있음. 실행 전 package.json의 현재 스크립트를 확인함. 빌드/실행 자체도 새 사용자 요청의 범위 안에서만 진행함.

## 10. 세션 용량 확인 기록과 한계

2026-09-09 08:45경 KST의 로컬 세션 메타데이터에서 context window 258,400 토큰, 직전 요청 입력 210,409 토큰(약 81%), 잔여 약 19%를 확인했음. 누적 JSONL 기록 파일은 약 322MiB였음. 누적 파일 크기, 현재 문맥, 계정 사용량 한도는 서로 다름.

이후 대화 자동 요약/압축으로 현재 입력 크기는 바뀔 수 있음. 09:00경 KST에 다시 확인한 입력은 102,628/258,400 토큰(약 40%)이었으며 누적 로그는 약 333MiB였음. 현재 문맥은 줄어도 누적 파일은 커질 수 있음. 이 값은 고정된 실시간 계기판 값이 아니라 해당 시점 기록임. 누적 사용 토큰 총합을 현재 문맥 사용량으로 계산하지 않음. 정확한 현재 수치를 다시 요청하면 최신 token_count 메타데이터만 읽으며 원문 로그나 비밀키를 출력하지 않음.

이번 백업은 파일·계획·작업 결정의 복구 수단임. 앱 자체 오류나 대화 서비스 장애가 절대로 발생하지 않음을 보장하는 기능은 아님. 브라우저 저장 여행 데이터는 별도 범위임.

## 2026-09-10 · 장소별 소통 하단 팝업 계획

랜드마크·음식점·호텔 뷰의 좋아요 수·댓글 수를 누르면 각각 하단 팝업에서 반응 확인·댓글·답글 작성을 제공하는 구성안을 추가함. 요약 8쪽·전체 33쪽에 동일한 구성 페이지를 반영함. 3차 F19는 샘플 화면, B3는 실제 회원·저장·중복 방지·집계·신고·차단임. 장소 ID별 집계와 여행기 반응을 분리함. 서비스 기능 개발이나 로컬 사진 모션 공개는 진행하지 않음.

## 2026-09-10 · 랜드마크 사진 넘겨보기를 1차에 포함

사용자 요청에 따라 인스타그램형 사진 이야기를 1차 F20에 명시함. 장소 메뉴의 ‘사진’ 보기에서 1~5장 사진·짧은 설명을 좌우로 넘기고, 세로로 다음 랜드마크를 봄. 사진 순번·장소명·지역·저장 상태를 표시함. 기존 영상·안내 목록을 유지하며 같은 장소 ID와 저장함을 사용함. 사진 자동 모션 실험은 별도이며 이번 공개 변경에 포함하지 않음. 개발 요약·전체 문서와 로드맵에 반영했으며 실제 기능 구현은 착수하지 않음.

## 2026-09-10 · 카드별 스레드 2차 배치 및 대상 정정

최신 사용자 요청은 여행기 본문에 삽입한 업체·랜드마크 카드 각각의 좋아요·댓글·답글임. F19를 3차에서 2차로 이동함. 앞서 적은 장소 ID별 전역 집계는 폐기하고 storyId + blockId로 분리함. 카드 이동은 반응 유지, 복사는 새 ID·0건이며 여행기 전체 댓글·업체 공통 리뷰와 구분함. 요약본 5쪽과 전체 24쪽에 같은 구현 방향을 기록함. 실제 저장·권한·집계는 B3, 이번 작업은 문서 수정만 수행함.

## 최신 페이지·기능 구조도

개발 요약 11쪽·전체 36쪽으로 개정함. 같은 구조도 3쪽을 요약 3–5쪽·전체 12–14쪽에 추가함: 메뉴·하위 페이지, 여행기 화면 구성, 기능 연결과 1·2·3차 로드맵. F20 사진 탐색은 1차, F19 여행기 카드별 스레드는 2차임. 카드 소통 상세는 요약 8쪽·전체 27쪽이며 이전 페이지 링크는 이 최신 위치를 우선함. 서비스 기능은 개발하지 않음.
