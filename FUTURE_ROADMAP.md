# Spotlog 남은 제품 개발 로드맵

기준일: 2026-09-05

이 문서는 이미 만들어진 모바일웹 데모를 다시 나열하지 않고, Spotlog를 실제 사용자에게 제공하기 위해 **앞으로 새로 구현하거나 상용 수준으로 교체해야 하는 작업만** 정의합니다.

## 상태 구분

- `기준선`: 현재 화면과 로컬 데모가 있어 재사용할 수 있음
- `부분 구현`: 사용자 흐름은 있으나 서버·권한·동기화가 없음
- `미구현`: 새 설계와 개발이 필요함
- `출시 차단`: 완료하지 않으면 공개 서비스나 앱스토어 출시를 진행하지 않음

## 1. 현재 기준선

다음 항목은 다시 만드는 대상이 아닙니다.

- 추천 일정과 여행자 여행기가 롤링되는 홈
- 지역·작성자·여행 기간으로 공개 여행기를 찾는 여행기 화면
- 영상과 지역 안내로 랜드마크를 발견하고 저장하는 장소 화면
- 지역별 저장 장소와 저장 해제
- 저장 장소의 거리·체류시간을 계산하는 로컬 일정 생성기
- 여러 여행 생성, DAY 추가·삭제, 글·사진·장소 블록 편집
- 숙소·맛집·카페·랜드마크 직접 등록
- 공개 여행 상세, 전체 여행 복사, 담김 수, 응원, 댓글, 작성자 등급
- MapLibre·OpenStreetMap·OSRM을 이용한 개발용 지도와 경로
- 브라우저 공유, 앱 공유 시트, 시스템 뒤로가기
- 알림 권한 UI와 로컬 테스트 알림
- GitHub Pages 모바일웹과 Expo WebView APK

현재 기능은 대부분 브라우저 `localStorage`에서만 동작합니다. 다른 사용자와 공유되는 회원·여행·댓글·담김 데이터, 클라우드 사진, 원격 푸시, 실제 AI와 장소 검색은 아직 없습니다.

## 2. 제품 목표

Spotlog의 핵심은 한국을 잘 아는 사람이 작성한 여행기를 다른 사람이 실제 일정으로 가져가 따라 여행하는 것입니다.

### 국내 사용자

1. 영상·장소 검색·직접 입력으로 장소를 발견
2. 여행별 보관함에 장소를 저장
3. 저장 장소로 일정 초안을 생성
4. 실제 여행 중 사진과 메모를 남김
5. 글·사진·장소·동선이 결합된 여행기를 발행
6. 다른 사용자가 여행 전체·DAY·장소를 자기 여행으로 가져감

### 외국인 관광객

1. 자기 언어로 한국의 실제 여행기를 발견
2. 한글 원래 장소명과 번역된 설명을 함께 확인
3. 장소를 저장하고 일정 전체 또는 일부를 복사
4. 지하철 출구, 이동 방법, 예약 필요 여부, 현지 주소를 이해
5. Google Maps·Apple Maps·카카오맵 등 사용 가능한 지도 앱으로 이동
6. 인터넷이 불안정해도 저장한 일정과 한글 주소를 확인
7. 잘못되거나 오래된 정보를 신고하고 원문을 확인

첫 다국어 출시 범위는 `한국어 + 영어`입니다. 그다음은 `일본어`, `중국어 간체`, `중국어 번체` 순서로 확장합니다.

## 3. 제품·기술 원칙

### 하이브리드 앱의 역할

모바일웹은 여행기 탐색·작성·공개 페이지의 본체로 유지합니다. 앱은 단순히 웹 주소만 여는 셸에 머물지 않고 다음 기능을 책임집니다.

- 촬영과 미디어 업로드
- 현재 위치와 실제 여행 모드
- 저장한 여행의 오프라인 열람과 동기화 대기열
- 푸시 알림과 알림을 통한 특정 여행기 진입
- 앱 링크·유니버설 링크
- OS 공유 시트와 설치된 지도 앱 선택

Apple은 재포장한 웹사이트보다 높은 앱 고유 기능을 요구하고, Google Play도 의미 있는 모바일 기능과 안정적인 경험을 요구합니다. 따라서 오프라인 여행 보관과 현장 여행 기능을 갖추기 전에는 현재 WebView APK를 최종 스토어 앱으로 취급하지 않습니다.

### 데이터 공급자의 역할 분리

- `Kakao Local`: 국내 업체명, 카테고리, 주소, 좌표, 전화번호, 카카오맵 상세 URL
- `한국관광공사 관광콘텐츠`: 다국어 관광지 설명과 사용 허가 범위가 명확한 관광 이미지
- `Google Places` 선택 연동: 외국어 장소명·주소, 해외 사용자의 검색 보조, 사진이 꼭 필요한 경우
- `사용자 입력`: 실제 경험, 평가, 사진, 숙소 선택 이유, 이동 팁
- `경로 공급자`: 거리·예상 이동시간·경로선
- `AI`: 일정 분배, 번역 초안, 글 구조화

카카오 장소 검색 응답에는 장소명·주소·좌표·전화·상세 URL은 있지만 장소 사진과 다국어 요청 필드는 없습니다. 카카오 하나만으로 외국인용 장소 콘텐츠를 완성하지 않습니다.

## P0. 서버와 데이터 기반 — 출시 차단

### 4. 회원·권한·서버 저장

초기 백엔드 후보는 Supabase의 PostgreSQL, Auth, Storage, Edge Functions입니다. 공급자는 실제 예상 사용량과 비용을 확인한 뒤 확정합니다.

구현 항목:

- 로그인 없이 공개 여행기 열람 가능
- 이메일 OTP, Google, Apple 로그인 우선 제공
- 국내 사용자를 위한 카카오 로그인은 선택 추가
- 계정 생성 없이 장소를 둘러볼 수 있는 게스트 모드
- 첫 로그인 시 기존 `localStorage` 여행과 저장 장소를 계정으로 이전
- 로그인 세션 만료·갱신·다른 기기 로그인 처리
- 앱과 모바일웹이 같은 계정·데이터 사용
- 앱 안에서 계정 삭제 요청과 데이터 내보내기 제공
- 개발·스테이징·운영 환경과 키를 분리

권장 핵심 테이블:

- `profiles`
- `journeys`
- `journey_days`
- `story_blocks`
- `media_assets`
- `places`
- `place_provider_refs`
- `place_translations`
- `journey_translations`
- `saved_collections`
- `saved_places`
- `journey_copies`
- `comments`
- `reactions`
- `follows`
- `push_tokens`
- `notification_events`
- `view_events`
- `reports`
- `user_blocks`
- `moderation_actions`

권한 원칙:

- 발행된 여행기만 비회원에게 읽기 허용
- 비공개 여행과 임시 저장본은 소유자만 접근
- 원본 작성자만 여행기 수정·삭제 가능
- 댓글 작성자는 자기 댓글만 수정·삭제 가능
- 담김·조회수·등급은 클라이언트가 숫자를 직접 변경하지 못하게 서버 함수로 집계
- 모든 노출 테이블에 RLS와 최소 권한을 적용
- `service_role`과 카카오·Google REST 키는 앱과 모바일웹에 넣지 않음
- 허용·거부 사례를 포함한 RLS 자동 테스트 작성

완료 조건:

- 같은 계정으로 두 기기에서 로그인했을 때 여행·저장 장소·프로필이 동일함
- 다른 사용자의 비공개 여행 ID를 알아도 읽거나 수정할 수 없음
- 로그아웃 후에도 공개 여행기는 볼 수 있지만 작성·댓글은 로그인 안내가 나옴
- 기존 로컬 여행 이전을 다시 실행해도 중복 데이터가 생기지 않음

### 5. 사진·영상 업로드 파이프라인

구현 항목:

- 사진 원본, 화면용 이미지, 썸네일을 별도 자산으로 관리
- 사용자별 업로드 경로와 Storage RLS 적용
- 6MB가 넘거나 네트워크가 불안정한 업로드는 재개 가능한 방식 사용
- 업로드 진행률·중단·재시도·취소 표시
- 사진 회전 정보 정규화와 공개본의 GPS EXIF 제거
- 이미지 포맷·해상도·용량 제한
- 영상 길이·용량·코덱 검증, 썸네일과 스트리밍용 변환
- 삭제한 여행기의 미사용 자산을 유예 기간 뒤 정리
- 저작권 및 촬영자 정보를 기록할 수 있는 필드

완료 조건:

- 업로드 중 앱을 닫거나 통신이 끊겨도 다시 이어서 올릴 수 있음
- 다른 사용자가 비공개 원본 URL을 추측해 열 수 없음
- 여행기 목록은 원본이 아니라 최적화된 썸네일을 사용

### 6. 로컬 데이터 서버 이전

현재 UI에서 사용하는 상태 계약을 유지하면서 저장소만 교체합니다.

- `localStorage` 접근을 저장소 인터페이스 뒤로 이동
- 서버 우선 조회와 낙관적 업데이트
- 요청별 로딩·실패·재시도 UI
- 오프라인 변경을 임시 저장하고 온라인 복귀 후 동기화
- 동일 항목 동시 수정 시 충돌 규칙과 마지막 수정자 표시
- 데이터 스키마 버전과 순차 마이그레이션

## P1. 장소 검색과 여행별 보관함

### 7. 장소 공급자 어댑터

장소 검색은 다음 계약으로 통일합니다.

```text
placeId
provider
providerPlaceId
nameKo
localizedNames
category
addressKo
localizedAddresses
lat
lng
phone
providerUrl
websiteUrl
sourceUpdatedAt
lastVerifiedAt
```

구현 항목:

- 카카오 키워드·카테고리·현재 지도 중심 반경 검색
- REST 키를 보관하는 서버 검색 함수
- IP 제한, 사용자별 호출 제한, 캐시, 쿼터 경보
- 검색 결과 페이지네이션과 입력 디바운스
- 공급자 장소와 기존 Spotlog 장소 중복 후보 제시
- 같은 장소의 카카오·관광공사·Google 식별자를 하나의 Spotlog 장소로 연결
- 폐업·이전·이름 변경을 원본 여행기와 분리해 표시
- 검색 결과가 없으면 지도 핀 또는 주소로 직접 등록
- 공급자 정책에 따라 저장 가능한 필드와 새로 조회해야 하는 필드 구분

Google Places는 언어 지정과 사진을 제공할 수 있지만 요청 필드에 따라 비용이 달라지므로 기본 공급자로 무조건 호출하지 않습니다. 영어 검색 품질 검증 후 선택적으로 사용합니다.

완료 조건:

- `성수 카페`, `Seongsu cafe`, `서울숲` 검색이 적절한 장소 후보를 반환
- 사용자가 선택한 장소의 한글명·주소·좌표가 여행기에 고정 저장됨
- 공급자 장애 시 직접 등록과 기존 저장 장소 사용이 가능

### 8. 여행별 장소 보관함

현재 하나로 섞인 저장 목록을 이름 있는 컬렉션으로 바꿉니다.

예시:

- 제주 가을 여행
- 서울 하루 산책
- 부모님과 강릉
- 언젠가 갈 곳

각 보관함에 저장할 정보:

- 여행 기간과 실제 날짜
- 출발·종료 위치
- 숙소 또는 숙박 지역
- 교통수단
- 여행 속도
- 동행자 유형
- 반드시 갈 장소와 제외할 장소
- 관심 주제와 식사·접근성 조건

구현 항목:

- 장소를 담을 때 보관함 선택 또는 새 보관함 생성
- 장소를 여러 보관함에 중복 저장 가능
- 보관함 간 장소 이동·복사
- 보관함별 정렬과 메모
- AI 일정은 사용자가 선택한 한 보관함만 입력으로 사용

완료 조건:

- 제주와 서울 장소를 따로 저장하고 각 보관함으로 서로 다른 일정을 만들 수 있음
- 어느 여행의 몇 번째 DAY에 들어갔는지 저장 카드에서 확인하고 해제 가능

## P2. 외국인용 로컬 여행 가이드

### 9. 다국어 UI 기반

구현 항목:

- 앱과 웹에서 기기 언어를 감지하되 사용자가 언제든 언어 변경 가능
- UI 문구와 사용자 콘텐츠 번역을 서로 분리
- URL과 공유 링크에 언어 정보 포함
- 날짜, 시간, 숫자, 거리, 온도를 사용자 로케일에 맞게 표시
- 한국 시간 기준 일정임을 명확히 표시
- 앱 이름·권한 설명·알림 채널·스토어 설명도 현지화
- 긴 영어·독일어 문장과 CJK 글자에서 깨지지 않는 레이아웃
- 접근성 라벨, 글자 확대, 색 대비, 스크린리더 점검

1차 지원 언어:

- `ko-KR`
- `en`

2차 지원 언어:

- `ja`
- `zh-Hans`
- `zh-Hant`

### 10. 다국어 장소와 관광 콘텐츠

한국관광공사 관광콘텐츠랩의 다국어 관광 콘텐츠와 이미지 API를 관광지 기준 데이터로 검토합니다. 현재 콘텐츠랩은 한국어, 영어, 일본어, 중국어 간체·번체를 포함한 여러 언어 자료를 제공합니다.

표시 원칙:

- 번역명만 보여주지 않고 한글 원래 이름을 함께 표시
- 택시 기사나 현지인에게 보여줄 `한글 이름·한글 주소 크게 보기`
- 장소명·주소 복사 버튼
- 전화번호는 `+82` 국제 형식도 제공
- 관광공사·장소 공급자·사용자 작성 정보를 출처별로 분리
- 공식 정보와 사용자 경험을 시각적으로 구분
- 영업시간·예약 방식처럼 변하는 정보에 확인 날짜 표시
- 잘못된 번역과 폐업 정보를 신고할 수 있는 버튼

외국인에게 필요한 여행기 필드:

- 가장 가까운 역·정류장과 정확한 출구
- 도보로 찾기 어려운 마지막 구간 설명
- 대중교통·택시·도보 중 추천 이동 방식
- 예약 필요 여부와 예약 가능한 언어
- 현금·카드·해외카드 사용 관련 작성자 경험
- 짐 보관, 화장실, 유아 동반, 휠체어 접근 정보
- 채식·할랄·알레르기 관련 작성자 확인 내용
- 사진 촬영·사유지·주거지역 예절
- 마지막 확인일과 정보 신고

작성자 경험은 공식 사실처럼 단정하지 않고 `작성자가 방문한 당시의 경험`으로 표시합니다.

### 11. 번역 작업 흐름

- 원문 언어를 항상 저장하고 원문 보기 제공
- 작성자가 직접 번역하거나 자동번역 초안을 검토해 발행
- 자동번역에는 `자동 번역` 배지와 피드백 버튼 표시
- 장소명·역명·음식명은 공통 용어집으로 표기 통일
- 주소·시간·요금·교통편은 자유 번역문에서 추출하지 않고 구조화 필드 사용
- 원문 수정 시 번역본을 `재검토 필요` 상태로 변경
- 번역되지 않은 댓글은 요청 시 번역하고 원문을 함께 제공

### 12. 외국인 지도·길찾기

하나의 지도 앱을 강제하지 않습니다.

- 앱 내 지도: 경로와 순서를 이해하는 용도
- Google Maps URL: 해외 사용자에게 익숙한 검색·길찾기 진입
- Apple Maps: iOS 기본 선택지
- 카카오맵·네이버지도: 국내 상세 장소 확인 선택지
- 한글 주소 복사: 택시 앱과 현지인에게 보여주기 위한 최후 수단

Google Maps URL은 API 키 없이 플랫폼 공통 링크로 열 수 있습니다. 장소 검색 API와 앱 내 경로 API 비용은 별도로 관리합니다.

구현 항목:

- 사용자별 기본 지도 앱 저장
- 장소 상세에서 `길찾기 앱 선택` 제공
- 출발지 생략 시 현재 위치를 지도 앱에서 선택
- 한글명과 번역명을 함께 넘기고 좌표를 우선 사용
- 대중교통·도보·차량 경로를 구분
- 앱 내 예상 경로와 실제 길찾기 결과가 다를 수 있음을 표시
- 공급자 약관 확인 없이 지도 타일을 오프라인 저장하지 않음

### 13. 여행 필수·안전 정보

여행기 화면을 방해하지 않는 별도 `한국 여행 도움말` 영역을 제공합니다.

- 경찰 `112`, 화재·구급 `119`
- 한국관광공사 여행안내 `1330`, 해외 발신 `+82-2-1330`
- 다국어 긴급·관광 안내 서비스 연결
- 현재 위치의 한글 주소 크게 표시
- 인터넷이 없어도 기본 긴급 번호와 저장한 주소 확인
- 의료·비자·재난 정보는 Spotlog가 임의 작성하지 않고 공식 출처로 연결

외국인 MVP 완료 조건:

- 영어 사용자가 한국어 UI를 거치지 않고 여행기 검색부터 일정 저장까지 완료
- 여행기의 모든 장소에서 한글 원명·주소와 영어 설명을 확인
- 저장한 여행을 오프라인에서 열고 한글 주소를 복사
- Google Maps 또는 다른 설치 지도 앱으로 장소 이동
- 자동번역과 공식 정보, 작성자 경험을 구분할 수 있음

## P3. 편집·복사·AI 완성

### 14. 편집 안정성

현재 수동 저장 편집기에 다음을 추가합니다.

- 1~2초 디바운스 자동 저장
- 저장 중·저장됨·오프라인·충돌 표시
- 마지막 변경 취소와 다시 실행
- DAY 복제
- 장소와 블록을 다른 DAY로 이동
- 사진 여러 장 선택과 업로드 대기열
- 임시 버전과 발행본 버전 복구
- 대표 사진 변경과 초점 위치 선택
- 발행 전 필수 항목·깨진 이미지·빈 장소 검사
- 브라우저나 앱을 강제 종료한 뒤 작성 위치 복구

### 15. 전체·DAY·장소 단위 복사

- 여행 전체 담기
- 선택한 DAY만 담기
- 장소 하나만 선택한 보관함 또는 DAY에 담기
- 중복 장소 발견과 `그래도 추가` 선택
- 원본 작성자와 원문 링크 표시
- 원본 변경 시 내 사본을 자동 덮어쓰지 않고 변경 알림 제공

복사 출처 필드:

```text
sourceJourneyId
sourceDayId
sourcePlaceId
sourceAuthorId
copiedAt
sourceRevision
```

담김 수는 같은 사용자의 반복 실행과 네트워크 재시도로 중복 증가하지 않도록 고유 이벤트로 기록합니다.

### 16. 서버 기반 AI 일정

현재 거리순 로컬 알고리즘을 안전한 기본값으로 유지하고, 서버 AI를 선택적으로 추가합니다.

입력:

- 보관함 장소
- 여행 날짜와 날짜별 사용 가능 시간
- 출발·종료 위치
- 숙소
- 교통수단
- 여행 속도
- 동행자와 접근성 조건
- 반드시 갈 곳·제외할 곳
- 확인 가능한 영업시간

처리 원칙:

- 좌표·경로·이동시간은 지도·경로 API 결과 사용
- 업체명·주소·영업정보는 장소 공급자 또는 사용자 입력만 사용
- AI가 업체 사실·가격·운영시간을 새로 만들지 못하게 구조화 출력 검증
- 경로 계산 실패와 영업시간 미확인 장소를 경고
- 생성 결과마다 입력 근거와 수정 가능 상태 표시
- 사용자별 생성 횟수 제한, 비용 상한, 실패 시 로컬 알고리즘 대체
- AI 문장과 번역에 신고·재생성·원문 확인 제공

## P4. 앱 전용 실제 여행 모드

### 17. 오프라인 여행 보관

앱이 단순 WebView가 아닌 여행 도구가 되기 위한 필수 기능입니다.

- 선택한 여행을 기기 SQLite에 다운로드
- 일정·장소·좌표·한글 주소·작은 이미지·긴급 정보 저장
- 원본 전체 사진과 지도 타일은 기본 다운로드 대상에서 제외
- 오프라인에서 방문 완료·메모·사진 대기열 작성
- 온라인 복귀 시 변경 내역 동기화
- 마지막 동기화 시각과 오프라인 데이터 용량 표시
- 여행 종료 후 다운로드 자료 삭제 선택

### 18. 현장 여행 모드

- 앱을 사용하는 동안에만 전경 위치 권한 요청
- 현재 위치에서 다음 장소까지 직선거리와 외부 길찾기 버튼
- 장소 도착·건너뛰기 완료 체크
- 사진과 한 줄 메모 빠른 기록
- 방문하지 못한 장소를 다음 DAY나 보관함으로 이동
- 하루 종료 시 사진·메모를 여행기 블록 초안으로 정리
- 배터리 절약 모드와 위치 권한 거부 시 수동 완료 대체

백그라운드 위치 추적은 기본 범위에서 제외합니다. 명확한 사용자 가치와 동의·배터리·스토어 심사 검토가 끝난 뒤 별도 결정합니다.

### 19. 공개 링크와 딥링크

- `https://spotlog.kr/j/{slug}` 여행기
- `https://spotlog.kr/u/{username}` 작성자
- `https://spotlog.kr/p/{placeId}` 장소
- 앱 설치 시 해당 화면을 여는 Android App Links·iOS Universal Links
- 앱이 없으면 동일한 모바일웹 콘텐츠 표시
- 링크 언어 파라미터와 기본 언어 협상
- 삭제·비공개 전환 콘텐츠의 정상 오류 화면
- 검색엔진과 소셜 공유용 제목·설명·대표 이미지

현재 GitHub Pages 해시는 개발용으로 유지할 수 있지만, 공개 콘텐츠 식별자는 영구 URL과 서버 ID로 전환합니다.

## P5. 실제 소셜·푸시·운영 안전

### 20. 서버 소셜 기능

- 실제 조회수, 담김, 응원, 댓글 집계
- 동일 사용자·세션의 반복 조회 필터
- 댓글 수정·삭제와 답글
- 작성자 댓글 좋아요와 고정
- 작성자 팔로우
- 활동 알림함과 읽음 상태
- 최근 본 여행기와 저장 기록
- 작성자 등급을 서버 집계 결과로 표시
- 담김 전환율·완독률·신고율을 함께 고려한 추천

숫자가 없는 초기 서비스에서는 가짜 조회수나 가짜 반응을 실제 수치처럼 표시하지 않습니다. 샘플 콘텐츠는 명확히 `샘플` 또는 `Spotlog 추천`으로 표시합니다.

### 21. 원격 푸시

- Expo Push Token과 사용자·기기·앱 버전 연결
- 토큰 갱신과 만료 토큰 정리
- 담김, 댓글, 답글, 응원, 팔로우 작성자의 새 글, 출발 전 알림
- 조회수 목표를 처음 통과한 경우에만 고유 이벤트 생성
- 알림 종류별 켜기·끄기와 조용한 시간
- 푸시를 누르면 해당 여행기·댓글·DAY로 이동
- 발송 티켓과 영수증 확인, 실패 재시도, 중복 방지
- 언어별 푸시 문구와 원문 콘텐츠 처리

원격 푸시는 서버, FCM/APNs 자격 증명, 실제 빌드, 기기 토큰이 모두 연결된 뒤 완료로 판단합니다. 현재 로컬 테스트 알림은 완료에 포함하지 않습니다.

### 22. 신고·차단·운영자 관리 — 공개 UGC 출시 차단

Apple과 Google은 공개 사용자 콘텐츠에 신고·차단·필터링과 지속적인 운영 대응을 요구합니다.

필수 기능:

- 가입·첫 발행 전에 커뮤니티 이용약관 동의
- 여행기·사진·댓글·사용자 신고
- 사용자 차단과 차단 해제
- 금칙어·스팸·과도한 반복 업로드 제한
- 신고 접수 상태와 운영자 처리 대기열
- 운영자 콘텐츠 숨김·복구·삭제
- 계정 경고·제한·정지
- 저작권 신고와 이의 제기
- 개인정보·주거지·민감 위치 노출 신고
- 처리 이력과 감사 로그
- 공개된 운영 연락처와 지원 채널

다국어 신고 사유를 표준 코드로 저장하고 운영자가 원문과 번역을 모두 확인할 수 있게 합니다.

## P6. 출시·운영 품질

### 23. 성능과 안정성

- 웹 화면과 지도 코드 분할 로딩
- 이미지 지연 로딩과 CDN 캐시 정책
- 긴 여행기·댓글·장소 목록 커서 페이지네이션
- API 타임아웃·재시도·회로 차단
- 앱 충돌, JS 오류, WebView 로드 실패 수집
- 장소 검색·경로·번역·AI 공급자별 성공률과 지연시간
- DB 백업과 실제 복원 훈련
- 데이터 마이그레이션 롤백 계획
- 주요 흐름 E2E 테스트와 실제 Android·iOS 기기 테스트
- 느린 네트워크와 해외 로밍 환경 테스트

### 24. 개인정보·스토어 정책

- 공개 개인정보처리방침과 이용약관
- 위치·사진·알림 권한을 필요한 순간에만 요청
- 선택 권한을 거부해도 수동 입력 경로 제공
- 계정과 관련 데이터 삭제, 내보내기, 보존 기간
- Google Play Data safety 작성
- Apple App Privacy 정보 작성
- WebView에서 전송하는 데이터와 제3자 SDK 수집도 함께 공개
- 관광공사·카카오·Google·지도 타일·사용자 미디어의 표시·저장·출처 정책 검토
- 앱스토어 심사용 데모 계정과 신고·차단 시나리오
- 회사 명의 전환 시 앱 소유권·도메인·키·스토어 계정 이전 계획

### 25. 접근성과 다국어 품질 검수

- 한국어와 영어 핵심 흐름 수동 검수
- 원어민 여행 용어 검수
- 글자 크기 확대와 스크린리더
- 키보드 탐색과 포커스 순서
- 자막 또는 영상 핵심 내용의 텍스트 대체
- 색상만으로 상태를 구분하지 않음
- 장소명·주소·노선명의 번역 일관성 검사

## 4. 출시 단계와 통과 기준

### 단계 A — 서버 기반 내부 알파

- 회원·DB·RLS·사진 업로드
- 로컬 데이터 이전
- 여행별 보관함
- 카카오 장소 검색 서버 함수
- 한국어 UI에서 기존 핵심 흐름 유지

### 단계 B — 국내 비공개 베타

- 자동 저장과 복구
- 전체·DAY·장소 복사
- 실제 공개 URL과 서버 조회·담김·댓글
- 신고·차단과 운영자 최소 화면
- 원격 푸시와 딥링크

### 단계 C — 영어 외국인 베타

- 한국어·영어 UI
- 한글 원명·주소와 영어 설명
- 한국관광공사 다국어 관광 콘텐츠 연결
- 지도 앱 선택과 한글 주소 복사
- 오프라인 여행 다운로드
- 현장 여행 모드와 112·119·1330 안내

### 단계 D — 공개 출시

- 스토어 정책·개인정보·데이터 삭제 완료
- 부하·보안·복원·실기기 테스트 통과
- 장소·지도·번역·AI 비용 경보
- 운영자 대응 절차와 연락처 공개
- 앱 고유 기능이 단순 웹 포장 수준을 넘는지 심사 전 점검

### 단계 E — 언어 확장

- 일본어
- 중국어 간체
- 중국어 번체
- 실제 이용자와 콘텐츠 수요를 보고 추가 언어 결정

## 5. 바로 시작할 구현 묶음

첫 작업은 `카카오 검색 화면`만 단독으로 붙이지 않습니다. 다국어와 서버 전환 시 다시 만들지 않도록 아래를 하나의 세로 기능으로 구현합니다.

1. Supabase 개발 환경과 마이그레이션 구성
2. `profiles`, `journeys`, `journey_days`, `story_blocks`, `places`, `saved_collections`, `saved_places` 생성
3. RLS와 허용·거부 테스트
4. 이메일 OTP·Google·Apple 계정 구조
5. 기존 로컬 여행을 서버 계정으로 한 번만 이전
6. 여행별 보관함 생성·선택 UI
7. 카카오 Local 서버 검색 함수와 직접 등록
8. `nameKo`, `addressKo`, `localizedNames`, `localizedAddresses`를 포함한 장소 계약
9. 한국어·영어 UI 프레임과 언어 선택
10. 영어 사용자 기준으로 장소 검색 → 저장 → 일정 생성까지 E2E 검증

이 묶음이 끝나야 이후 편집 안정화, 부분 복사, 실제 여행 모드, 원격 푸시가 같은 서버 데이터 위에서 한 번만 구현됩니다.

## 6. 당분간 제외할 기능

- 호텔 가격 비교와 객실 재고
- Spotlog 내부 예약·결제·정산
- 사용자 간 실시간 채팅
- 항상 켜지는 위치 추적
- 전국 업체 정보 자체 구축
- 자체 턴바이턴 내비게이션
- 공급자 약관을 확인하지 않은 이미지·지도 타일 저장
- AI가 업체 평가·가격·영업시간을 임의 생성하는 기능
- 실제 서버 집계 전 등급·포인트 체계 확장
- 모든 언어를 한 번에 지원하는 작업

## 7. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| `localStorage` 데이터 유실 | 첫 로그인 이전 도구, 서버 저장, 오프라인 동기화 |
| 카카오 장소에 이미지·다국어 부족 | 관광공사, 사용자 사진, 필요 시 Google Places 선택 연동 |
| 외국인이 한글 장소를 찾기 어려움 | 한글 원명·주소 병기, 복사, 좌표 우선, 지도 앱 선택 |
| 자동번역이 시간·주소·교통을 왜곡 | 구조화 필드, 원문 표시, 번역 상태와 신고 |
| 공개 UGC의 스팸·괴롭힘·저작권 문제 | 발행 전 약관, 신고, 차단, 필터, 운영자 도구 |
| WebView 앱이 스토어에서 가치 부족으로 판단 | 오프라인 여행, 위치, 촬영, 푸시, 딥링크를 앱 고유 기능으로 구현 |
| 장소·지도·AI 비용 급증 | 서버 키, 캐시, 사용자 제한, 쿼터·비용 경보, 공급자 대체 |
| 경로 정보가 실제 교통과 다름 | 예상치 표시, 확인 시각, 외부 길찾기 연결 |
| 외국인 안전 정보가 오래되거나 부정확 | 공식 출처 링크, 최소 정보 오프라인 저장, 확인일 표시 |

## 8. 참고한 공식 자료

- [Supabase Auth](https://supabase.com/docs/guides/auth)
- [Supabase Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase Storage 접근 제어](https://supabase.com/docs/guides/storage/security/access-control)
- [Supabase 재개 가능한 업로드](https://supabase.com/docs/guides/storage/uploads/resumable-uploads)
- [카카오 Local API](https://developers.kakao.com/docs/ko/local/dev-guide)
- [카카오 API 쿼터](https://developers.kakao.com/docs/ko/getting-started/quota)
- [한국관광공사 관광콘텐츠랩](https://api.visitkorea.or.kr/)
- [Google Places Text Search](https://developers.google.com/maps/documentation/places/web-service/text-search)
- [Google Maps URL](https://developers.google.com/maps/documentation/urls/get-started)
- [Expo SDK 57 Localization](https://docs.expo.dev/versions/v57.0.0/sdk/localization/)
- [Expo SDK 57 Notifications](https://docs.expo.dev/versions/v57.0.0/sdk/notifications/)
- [Expo Push Notifications](https://docs.expo.dev/push-notifications/overview/)
- [Expo Linking·App Links·Universal Links](https://docs.expo.dev/linking/overview/)
- [Expo SDK 57 Location](https://docs.expo.dev/versions/v57.0.0/sdk/location/)
- [Expo SDK 57 SQLite](https://docs.expo.dev/versions/v57.0.0/sdk/sqlite/)
- [Apple App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/)
- [Apple 계정 삭제 가이드](https://developer.apple.com/support/offering-account-deletion-in-your-app/)
- [Google Play 사용자 콘텐츠 정책](https://support.google.com/googleplay/android-developer/answer/9876937)
- [Google Play Data safety](https://support.google.com/googleplay/android-developer/answer/10787469)
- [VISITKOREA 1330 여행안내](https://english.visitkorea.or.kr/svc/contents/infoHtmlView.do?menuSn=454&vcontsId=140632)
